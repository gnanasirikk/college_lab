
function findAbsolute(n) {
  return new Promise((resolve, reject) => {
    if (typeof n === "number" && n >= 0) {
      resolve("Absolute value!!");
    } else {
      reject("Invalid");
    }
  });
}

// Async function
async function findResult(n) {
  try {
    const result = await findAbsolute(n);
    console.log(result);
  } catch (error) {
    console.log(error);
  }
}

// Call function (input value)
findResult(10);    // Absolute value!!
findResult(-5);    // Invalid



{% comment %} node app.js {% endcomment %}
